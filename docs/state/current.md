phase: 'Phase 2 — Scanner + Assessor + ModelRouter'
certified_floor: 33/0/0
what_is_built: 'Scanner (file walking), Assessor (cheap model calls), ModelRouter (env-var overridable), AssessmentResult TypedDict'
what_is_next: 'Phase 3 — Writer + ScopeGuard + StateWriter + CLI analyze command'
open_questions:
  - 'Which OpenRouter models for cheap/capable stage routing?'
  - 'SOUL.md interview — run openagent init on first use or bundled?'
