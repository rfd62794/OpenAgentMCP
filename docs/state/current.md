phase: 'Phase 3 — Writer + ScopeGuard + StateWriter + CLI'
certified_floor: 60/0/0
what_is_built: 'Writer (capable model calls), ScopeGuard (floor + intent validation), StateWriter (atomic writes), CLI analyze command'
what_is_next: 'Phase 4 — MCP server + serve command'
open_questions:
  - 'Which OpenRouter models for cheap/capable stage routing?'
  - 'SOUL.md interview — run openagent init on first use or bundled?'
